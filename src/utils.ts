export enum Routify {
    HOME = "/*",
    PROFILE = "/profile",
    PROFILEID = "/profile/:id",
    PROFILEEDIT = "/profile/:id/edit",
    CHANGEPASWWORD = "/profile/change-password",
    CREATEPOST = "/post/create",
    WALLET = "/profile/wallet",
}